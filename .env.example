GONG_ACCESS_KEY=
GONG_ACCESS_KEY_SECRET=
ANTHROPIC_API_KEY=

# For local testing only (agentops-run-worker --env-file .env). In a real
# deployment these are supplied to the worker as environment variables by the
# Kubernetes Deployment (see k8s/), not read from this file.
AGENTOPS_URL=
AGENTOPS_WORKER_TOKEN=
